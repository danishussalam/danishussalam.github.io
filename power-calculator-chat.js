/* Placeholder replaced by the chat panel in Task 10. Keeps the chat button inert until then. */
(function () {
  'use strict';
  const fab = document.getElementById('chatFab');
  if (fab) fab.hidden = true;
})();
